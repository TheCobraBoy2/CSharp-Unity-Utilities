{
  "name": "csharp-unity-utilities",
  "displayName": "C# Unity Utilities",
  "publisher": "Landon Snellgrove",
  "version": "0.0.5",
  "engines": {
    "vscode": "^1.80.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/TheCobraBoy2/CSharp-Unity-Utilities"
  },
  "scripts": {
    "changelog": "auto-changelog -p",
    "update-readme": "node update-readme.js",
    "prebuild": "npm run changelog && npm run update-readme && git add . && git commit -m \"chore: update changelog and README\" && npm version patch",
    "build": "npm run prebuild && mkdir -p build && vsce package --out build"
  },
  "categories": [
    "Other"
  ],
  "main": "extension.js",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "C# Unity Utilities",
      "properties": {
        "csharpUnityUtilities.template": {
          "type": "string",
          "default": "// Auto-generated MonoBehaviour\nusing UnityEngine;\n\nnamespace {{NAMESPACE}}\n{\n    public class {{CLASSNAME}} : MonoBehaviour\n    {\n        private void Start() {}\n        private void Update() {}\n    }\n}",
          "description": "Template for new MonoBehaviour scripts. Use {{NAMESPACE}} and {{CLASSNAME}} as placeholders."
        }
      }
    },
    "commands": [
      {
        "command": "unityUtilities.newMonoBehaviour",
        "title": "New C# MonoBehaviour Script"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "unityUtilities.newMonoBehaviour",
          "group": "navigation"
        }
      ]
    }
  }
}
